# gpio-hal-printbuttons: button interrupt handler demo

Demonstrate the setup and use of a button interrupt, using
RTT tracing prints.

Note: This example is taken from the `microbit` crate
source, and lightly modified to work.
