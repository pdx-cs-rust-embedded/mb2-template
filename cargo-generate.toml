[placeholders]
starter_type = { prompt = "Starter type", choices = ["Blinky", "Hello world"], default = "Blinky", type = "string" }
gdb_enabled = { prompt = "Enable GDB", default = false, type = "bool" }
